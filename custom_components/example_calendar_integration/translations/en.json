{
  "config": {
    "step": {
      "user": {
        "title": "Example Calendar Integration",
        "description": "Do you want to set up the Example Calendar Integration?"
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    }
  }
}
