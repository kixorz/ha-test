{
  "config": {
    "step": {
      "user": {
        "title": "Local Calendar Status Example",
        "description": "Do you want to set up the Local Calendar Status Example?"
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    }
  }
}
